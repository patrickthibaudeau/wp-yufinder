{{#platforms}}

      <label class="btn-checkbox">
              <input type="checkbox" onclick="toggleColumn('platform-{{platformid}}')" checked>
              <span class="btn-checkbox-label">{{name}}</span>
              <span class="checkmark"></span>
          </label>

 {{/platforms}}